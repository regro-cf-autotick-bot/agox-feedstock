{% set name = "agox" %}
{% set version = "3.9.0" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/agox-{{ version }}.tar.gz
  sha256: c4e4b1f592d20253216b095a204ee7dcff21df967424a1de26934fe4421f5e46

build:
  script: {{ PYTHON }} -m pip install . -vv --no-deps --no-build-isolation
  number: 0
  skip: true  # [win]

requirements:
  build:
    - {{ compiler('c') }}
    - {{ stdlib("c") }}
  host:
    - python
    - setuptools
    - wheel
    - cython
    - cymem
    - numpy
    - pip
  run:
    - ase
    - python
    - cymem
    - h5py
    - matscipy
    - scikit-learn
    - numpy <1.24
    - {{ pin_compatible('numpy') }}

test:
  imports:
    - agox
  commands:
    - pip check
  requires:
    - pip

about:
  home: https://agox.gitlab.io/agox/
  summary: Atomistic Global Optimziation X is a framework for structure optimization in materials science.
  license: GPL-3.0-or-later
  license_file: LICENSE.txt

extra:
  recipe-maintainers:
    - awvwgk
