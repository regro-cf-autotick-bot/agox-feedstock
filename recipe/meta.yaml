{% set name = "agox" %}
{% set version = "3.8.0" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/agox-{{ version }}.tar.gz
  sha256: d96a09c0b741e2e5ac58907d10573bd1a52f4cfab6024234e3cab8691651090b

build:
  script: {{ PYTHON }} -m pip install . -vv --no-deps --no-build-isolation
  number: 0
  skip: true  # [win]

requirements:
  build:
    - {{ compiler('c') }}
    - {{ stdlib("c") }}
  host:
    - python
    - setuptools
    - wheel
    - cython
    - cymem
    - numpy
    - pip
  run:
    - ase
    - python
    - cymem
    - h5py
    - matscipy
    - scikit-learn
    - numpy <1.24
    - {{ pin_compatible('numpy') }}

test:
  imports:
    - agox
  commands:
    - pip check
  requires:
    - pip

about:
  home: https://agox.gitlab.io/agox/
  summary: Atomistic Global Optimziation X is a framework for structure optimization in materials science.
  license: GPL-3.0-or-later
  license_file: LICENSE.txt

extra:
  recipe-maintainers:
    - awvwgk
